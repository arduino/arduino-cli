workspace:
  base: /go
  path: src/github.com/bcmi-labs/arduino-cli

pipeline:
  build:
    image: golang:latest
    commands:
      - go build -o arduino
      - go test ./... -race
    secrets: [TEST_USERNAME, TEST_PASSWORD]
