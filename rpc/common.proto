syntax = "proto3";

package arduino;

option go_package = "github.com/arduino/arduino-cli/rpc";

message Instance { int32 id = 1; }

message TaskProgress {
    string name = 1;
    string message = 2;
    bool completed = 3;
}
